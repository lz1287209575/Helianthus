load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "scripting_test",
    srcs = ["ScriptingTest.cpp"],
    deps = [
        "//Shared/Scripting:scripting",
        "@googletest//:gtest_main",
    ],
    data = [
        "//Scripts:hello.lua",
    ],
    copts = [
        "/std:c++20",
    ] + select({
        "@bazel_tools//tools/cpp:msvc": ["/utf-8"],
        "//conditions:default": ["-std=c++20", "-fPIC"],
    }),
)
